import {sum,subtract, Person} from "./task"

test('Sum of first and second', () => {
    expect(sum(1,2)).toBe(3)  
});

test('Subtract of first and second', () => {
    expect(subtract(4,1)).toBe(3)  
});



